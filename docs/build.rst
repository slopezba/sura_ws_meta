:orphan:

Build
=====

Always build from the workspace root.

.. code-block:: bash

   colcon build

After building, source the generated setup file as appropriate for the shell and
environment being used.
